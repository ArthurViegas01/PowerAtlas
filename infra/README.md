# Infrastructure — deferred

Terraform / deployment config is deferred until there is a backend to deploy
(Phase 2). The Phase 1 frontend is a static Vite build that can be hosted
anywhere. See ARCHITECTURE.md → "Deferred to Phase 2".
